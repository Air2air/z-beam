# Python dependencies for Z-Beam YAML Processor
# ==============================================

# Core dependencies (minimal requirements)
# No external dependencies required - uses only Python standard library

# Optional: For enhanced YAML validation (not currently used)
# PyYAML>=6.0.1

# Development dependencies
# pathlib2>=2.3.7; python_version < "3.4"  # Only for very old Python versions
