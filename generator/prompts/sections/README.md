# Section Prompts

This directory contains prompt templates for each article section type (e.g., introduction, chart, comparison, etc.).

All prompt loading should be done via the `PromptManager` class.
